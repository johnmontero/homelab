apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  annotations:
    metallb.io/loadBalancerIPs: 192.168.18.221
  name: homelab-gateway
  namespace: default
spec:
  gatewayClassName: eg
  listeners:
  - allowedRoutes:
      namespaces:
        from: All
    name: http
    port: 80
    protocol: HTTP
